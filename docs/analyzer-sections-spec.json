{
  "$comment": [
    "R1738 — WHICH SECTIONS OF THE ASSEMBLED APPLICATION ARE JUDGED, and for",
    "each that is not, the reason that was accepted.",
    "",
    "WHAT THIS PIN IS A CLAIM ABOUT, AND WHAT IT IS NOT. The sibling pins",
    "(`analyzer-rail-spec.json`, `analyzer-keys-spec.json`,",
    "`analyzer-logs-spec.json`, `analyzer-inspector-spec.json`,",
    "`analyzer-board-spec.json`) are claims about the BEHAVIOUR REFERENCE —",
    "what it draws, in neutral vocabulary, written from an extraction. This one",
    "is not. It is a claim about THIS BUILD: of the sections the reference",
    "opens, which ones this application can currently say anything about.",
    "",
    "It therefore does not repeat the seat list. The population is",
    "`analyzer-rail-spec.json`'s canon, read from there, and this file holds",
    "only the remainder — because a second copy of the eight seats is how two",
    "screens of this tool came to disagree about what the tool contains, and",
    "the fix for that was not another list.",
    "",
    "WHAT FORCED IT. Measured over the wire at R1738, standing in each section",
    "of the running application in turn: the application's own `conformance`",
    "slot answered `specified 8, reproduced 8` from every one of them. That is",
    "true about the RAIL. Read as a statement about the tool — which is how it",
    "reads — it was wrong: two of the six open sections published a verdict",
    "about their own surfaces, four published nothing, and no slot, test or",
    "report anywhere named the other four. They were not failing a check; they",
    "were absent from the population.",
    "",
    "HOW IT IS ENFORCED. `scene`-side, `ScreenRoster::conformance` builds one",
    "row per destination FROM THE ROSTER, so a section is missing from the",
    "report only by not being in the application. Gate-side, the entries below",
    "are asserted EQUAL to the application's unjudged rows — so a section that",
    "starts answering must have its entry DELETED here, and an entry left",
    "behind fails exactly as loudly as a section that went silent. A remainder",
    "list that only ever grows is a remainder list nobody reads.",
    "",
    "THE TWO KINDS BELOW ARE DIFFERENT WORK. `inline` is a page the host paints",
    "itself, so the roster has no screen to ask; closing it means giving that",
    "page a `Screen` of its own, which is what the trait being public is for.",
    "`unspecified` is a mounted screen that publishes no verdict — and the two",
    "entries of that kind are themselves different: one has no written",
    "specification at all, the other has one it checks itself against inside a",
    "unit test of its own binary, where the assembled application cannot see",
    "it. The framework cannot tell those apart, because only a screen knows;",
    "that a screen has no way to say so is this round's own remainder and is",
    "recorded at the bottom."
  ],
  "unjudged": {
    "owed": [
      {
        "key": "dashboard",
        "kind": "inline",
        "sentence": "the host paints this page itself, so there is no screen to ask",
        "since": "R1738",
        "why": [
          "The board is the shell's own view code rather than a mounted",
          "screen, so the roster has nothing to ask. The route is not a new",
          "mechanism: `pinion_screen::Screen` is public precisely for a host's",
          "inline page, and giving the board one would make it answerable the",
          "same way the four mounts are. It is a refactor of a 16.9k-line",
          "binding and is its own round."
        ]
      },
      {
        "key": "packets",
        "kind": "unspecified",
        "sentence": "a screen is here and it publishes no verdict about a specification",
        "since": "R1738",
        "why": [
          "The capture viewer has no written specification at all — not one",
          "unpublished, one unwritten. Its surfaces have never been extracted",
          "from the behaviour reference the way the key-pattern and log",
          "sections' were, which is the same gap already recorded as the",
          "capture viewer having no operation table. Writing that pin is the",
          "work, and it is a round of its own because the extraction is the",
          "expensive half."
        ]
      },
      {
        "key": "lab",
        "kind": "unspecified",
        "sentence": "a screen is here and it publishes no verdict about a specification",
        "since": "R1738",
        "why": [
          "★ The one that is NOT missing a specification. The node lab has",
          "`analyzer-inspector-spec.json` and already compares itself against",
          "it — inside a unit test of its own binary, where the assembled",
          "application cannot see it. It is also the hardest of the four to",
          "publish, and for a reason worth keeping: its built parts are read",
          "back out of a PAINTED scene through a test-only fixture, and the",
          "surfaces it judges only exist once a card with an enumeration row is",
          "selected. So its verdict is about a session rather than about the",
          "build, and answering `conformance` honestly means deciding what a",
          "screen says when the surface being judged is not on screen. That",
          "decision is the round, not the wiring."
        ]
      },
      {
        "key": "settings",
        "kind": "inline",
        "sentence": "the host paints this page itself, so there is no screen to ask",
        "since": "R1738",
        "why": [
          "The same shape as `dashboard` and it would be closed by the same",
          "edit. Listed separately rather than folded into one entry, because a",
          "ledger whose entries do not correspond one-to-one with the rows they",
          "excuse cannot be asserted equal to them."
        ]
      }
    ]
  },
  "it_is_the_application": {
    "owed": [
      {
        "key": "a-section-cannot-say-why-it-answers-nothing",
        "sentence": "an unjudged section's row carries the host's reason, not the screen's",
        "since": "R1738",
        "why": [
          "`SectionStanding::Unspecified` is what a host can see: this screen",
          "published no verdict. Whether that is because nobody wrote a",
          "specification or because one exists and is checked somewhere the",
          "application cannot reach is a fact only the screen has, and the hook",
          "it would answer through returns `Option<DocumentReport>` — which has",
          "room for a report and no room for a reason. Two of the four entries",
          "above are exactly this distinction, written here by hand because the",
          "type cannot carry it. Stated rather than papered over: the honest",
          "fix is a declared absence that carries its reason, the way a closed",
          "destination's does."
        ]
      },
      {
        "key": "a-verdict-is-not-recomputed-per-frame",
        "sentence": "the report is built on demand and nothing caches or invalidates it",
        "since": "R1738",
        "why": [
          "Every read walks the roster and asks each mounted screen, which for",
          "the two answering sections re-parses nothing but does rebuild their",
          "part rosters. It is a wire read at an agent's cadence rather than a",
          "paint-time cost, so it is not on any frame budget measured so far —",
          "but that is an argument from where it is called, not a measurement",
          "of what it costs, and it is written down as such."
        ]
      }
    ]
  }
}
