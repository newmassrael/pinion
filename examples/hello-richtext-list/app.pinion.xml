<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1559 §5.36 list consumer: pinion-forge renderer manifest for
  hello-richtext-list. Emits `HelloRichTextListRenderer` (Vello-backed
  RenderContext + RenderSurface + Renderer wrapper). Same emit template as
  hello-richtext-blocks; only the struct name diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloRichTextListRenderer"
        backend="vello"/>
