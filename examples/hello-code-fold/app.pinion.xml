<?xml version="1.0" encoding="UTF-8"?>
<!--
  R933 §5.36 — pinion-forge renderer manifest for hello-code-fold.
  Emits `HelloCodeFoldRenderer` (Vello-backed RenderContext + RenderSurface
  + Renderer wrapper). Identical manifest shape to hello-syntax-highlight's
  `HelloSyntaxRenderer` — single-source-of-truth emit template, only the
  struct name diverges. `aa` attribute omitted -> defaults to Area per
  R46.2.1 (UI-canonical mode).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloCodeFoldRenderer"
        backend="vello"/>
