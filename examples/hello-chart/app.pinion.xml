<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1354 Phase-B dataviz substrate consumer: pinion-forge renderer
  manifest for hello-chart. Emits `HelloChartRenderer` (Vello-backed
  RenderContext + RenderSurface + Renderer wrapper). Identical manifest
  shape to hello-path's `HelloPathRenderer` — single-source-of-truth emit
  template, only the struct name diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloChartRenderer"
        backend="vello"/>
