<?xml version="1.0" encoding="UTF-8"?>
<!--
  R740 §5.16 raster image: pinion-forge renderer manifest for
  hello-image. Same single-source-of-truth template that emits the
  other hello-* renderers; only the struct name diverges.
  backend=vello + default Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloImageRenderer"
        backend="vello"/>
