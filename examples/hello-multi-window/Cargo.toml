[package]
name = "hello-multi-window"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "R670.B §5.16 §5.41 first multi-window consumer — main Button + inspector Text mirror. First real dogfood of WidgetView::windows() + view_for_window + RPC {window: '<id>'} per-window scope (Phase B substrate Round 2-3)."
publish = false

[[bin]]
name = "hello-multi-window"
path = "src/main.rs"

[dependencies]
pinion-core = { path = "../../crates/pinion-core", version = "0.0.1" }
pinion-a11y = { path = "../../crates/pinion-a11y", version = "0.0.1" }
pinion-shell = { path = "../../crates/pinion-shell", version = "0.0.1" }
vello = { workspace = true }

[build-dependencies]
pinion-forge = { path = "../../crates/pinion-forge", version = "0.0.1" }

[lints]
workspace = true
