<?xml version="1.0" encoding="UTF-8"?>
<!--
  R827 §2 #4 §5.20 — pinion-forge codegen manifest for
  hello-immediate-intent. Emits a `HelloImmediateIntentRenderer`
  Vello-backed struct on the same template the other hello-* bindings
  use. This binding is the first-time consumer of the immediate-mode
  -> retained intent bridge (ImmediateMode::drain_intents +
  walk_scene_and_drain_immediate); the renderer wire itself is
  unchanged from the canonical template.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloImmediateIntentRenderer"
        backend="vello"/>
