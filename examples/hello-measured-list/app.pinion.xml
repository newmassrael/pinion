<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1194 §5.27 measured variable-height virtualization consumer:
  pinion-forge renderer manifest for hello-measured-list. Same
  single-source-of-truth template as the other hello-* renderers; only
  the struct name diverges. backend=vello + default Area antialiasing
  (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloMeasuredListRenderer"
        backend="vello"/>
