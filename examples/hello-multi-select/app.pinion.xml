<?xml version="1.0" encoding="UTF-8"?>
<!--
  R780 §5.40 multi-select index model: pinion-forge renderer manifest for
  hello-multi-select. Same single-source-of-truth template as the other
  hello-* renderers; only the struct name diverges. backend=vello +
  default Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloMultiSelectRenderer"
        backend="vello"/>
