<?xml version="1.0" encoding="UTF-8"?>
<!--
  R710 §5.50 drop-shadow substrate consumer: pinion-forge renderer
  manifest for hello-elevation. Emits `HelloElevationRenderer` (Vello-
  backed RenderContext + RenderSurface + Renderer wrapper). Identical
  manifest shape to hello-gradient's `HelloGradientRenderer` — single-
  source-of-truth emit template, only the struct name diverges. `aa`
  attribute omitted -> defaults to Area per R46.2.1 (UI-canonical mode).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloElevationRenderer"
        backend="vello"/>
