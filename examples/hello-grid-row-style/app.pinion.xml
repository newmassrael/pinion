<?xml version="1.0" encoding="UTF-8"?>
<!--
  R998 §5.40 per-row style rules at scale: pinion-forge renderer manifest for
  hello-grid-row-style. Same single-source-of-truth template as the other
  hello-* renderers; only the struct name diverges. backend=vello + default
  Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloGridRowStyleRenderer"
        backend="vello"/>
