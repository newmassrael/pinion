<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1008 §5.41 §5.49 pointer-to-cell hit-test: pinion-forge renderer manifest
  for hello-grid-pointer. Same single-source-of-truth template as the other
  hello-* renderers; only the struct name diverges. backend=vello + default
  Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloGridPointerRenderer"
        backend="vello"/>
