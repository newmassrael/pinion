<?xml version="1.0" encoding="UTF-8"?>
<!--
  R635 §5.7 — figma-button-m3 renderer manifest. Emits a
  `FigmaButtonM3Renderer` Vello-backed struct via the canonical
  pinion-forge renderer template. `aa` attribute omitted → defaults
  to Area per R46.2.1 (UI-canonical mode; Vello/Xilem documented
  default). Same shape as ai-introspect-demo / hello-* renderers —
  only the struct name differs per binding.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="FigmaButtonM3Renderer"
        backend="vello"/>
