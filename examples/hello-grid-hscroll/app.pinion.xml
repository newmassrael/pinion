<?xml version="1.0" encoding="UTF-8"?>
<!--
  R784 §5.45 horizontal scroll + frozen header: pinion-forge renderer
  manifest for hello-grid-hscroll. Same single-source-of-truth template
  as the other hello-* renderers; only the struct name diverges.
  backend=vello + default Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloGridHscrollRenderer"
        backend="vello"/>
