<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1545 Phase-B charting consumer: pinion-forge renderer manifest for
  hello-category-axis. Emits `HelloCategoryAxisRenderer` (Vello-backed
  RenderContext + RenderSurface + Renderer wrapper) — the same
  single-source emit template every chart example uses; only the struct
  name diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloCategoryAxisRenderer"
        backend="vello"/>
