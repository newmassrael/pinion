<?xml version="1.0" encoding="UTF-8"?>
<!--
  R844 §5.27 §5.40 stacked-proxy (filter -> sort -> group) consumer:
  pinion-forge renderer manifest for hello-grouped-sort. Same
  single-source-of-truth template as the other hello-* renderers; only the
  struct name diverges. backend=vello + default Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloGroupedSortRenderer"
        backend="vello"/>
