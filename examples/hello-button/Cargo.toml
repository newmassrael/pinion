[package]
name = "hello-button"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "First dogfood (§4): winit window + R12 Button SCXML statechart end-to-end"
publish = false

[[bin]]
name = "hello-button"
path = "src/main.rs"

[dependencies]
pinion-core = { path = "../../crates/pinion-core", version = "0.0.1" }
pinion-runtime = { path = "../../crates/pinion-runtime", version = "0.0.1" }
pinion-rpc = { path = "../../crates/pinion-rpc", version = "0.0.1" }
winit = { workspace = true }
softbuffer = { workspace = true }
# R21 §5.3 slice 7: cosmic-text Text rasterizer (Scene::Text → softbuffer).
# Example-scope only — pinion-core stays free of any rasterizer dep.
cosmic-text = "0.12"

[lints]
workspace = true
