<?xml version="1.0" encoding="UTF-8"?>
<!--
  R783 §5.40 data-grid filter at scale: pinion-forge renderer manifest for
  hello-grid-filter. Same single-source-of-truth template as the other
  hello-* renderers; only the struct name diverges. backend=vello +
  default Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloGridFilterRenderer"
        backend="vello"/>
