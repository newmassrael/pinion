<?xml version="1.0" encoding="UTF-8"?>
<!--
  R838 §5.16 node-graph editor: pinion-forge renderer manifest for
  hello-node-editor. Same single-source-of-truth template that emits the
  other hello-* renderers; only the struct name diverges.
  backend=vello + default Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloNodeEditorRenderer"
        backend="vello"/>
