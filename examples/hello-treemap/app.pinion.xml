<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1382 area-encoded part-of-whole chart consumer: pinion-forge renderer
  manifest for hello-treemap. Emits `HelloTreemapRenderer` (Vello-backed
  RenderContext + RenderSurface + Renderer wrapper). Identical manifest shape
  to hello-donut's `HelloDonutRenderer` — single-source-of-truth emit
  template, only the struct name diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloTreemapRenderer"
        backend="vello"/>
