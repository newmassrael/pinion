[package]
name = "hello-slider"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "R51.35 §5.38 paint-side N=5: Slider SCXML widget on the pinion-shell substrate, consuming R51.34's pointer-capture + pointer_move forward — first drag-aware Tier-1 widget visual demo"
publish = false

[[bin]]
name = "hello-slider"
path = "src/main.rs"

[dependencies]
pinion-core = { path = "../../crates/pinion-core", version = "0.0.1" }
pinion-shell = { path = "../../crates/pinion-shell", version = "0.0.1" }
vello = { workspace = true }

[build-dependencies]
pinion-forge = { path = "../../crates/pinion-forge", version = "0.0.1" }

[lints]
workspace = true
