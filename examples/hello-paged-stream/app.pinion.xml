<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1005 §5.22 §5.23 §5.27 paged out-of-memory streaming: pinion-forge renderer
  manifest for hello-paged-stream. Same single-source-of-truth template as the
  other hello-* renderers; only the struct name diverges. backend=vello +
  default Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloPagedStreamRenderer"
        backend="vello"/>
