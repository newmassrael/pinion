<?xml version="1.0" encoding="UTF-8"?>
<!--
  R833 — pinion-forge renderer manifest for hello-print. Same
  single-source-of-truth template as the other hello-* renderers; only
  the struct name diverges. backend=vello.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloPrintRenderer"
        backend="vello"/>
