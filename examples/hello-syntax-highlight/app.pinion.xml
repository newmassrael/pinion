<?xml version="1.0" encoding="UTF-8"?>
<!--
  R904 §5.36 — pinion-forge renderer manifest for hello-syntax-highlight.
  Emits `HelloSyntaxRenderer` (Vello-backed RenderContext + RenderSurface
  + Renderer wrapper). Identical manifest shape to hello-textfield's
  `HelloTextFieldRenderer` — single-source-of-truth emit template, only
  the struct name diverges. `aa` attribute omitted -> defaults to Area
  per R46.2.1 (UI-canonical mode).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloSyntaxRenderer"
        backend="vello"/>
