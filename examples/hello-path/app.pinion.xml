<?xml version="1.0" encoding="UTF-8"?>
<!--
  R721 §5.16 vector-path substrate consumer: pinion-forge renderer
  manifest for hello-path. Emits `HelloPathRenderer` (Vello-backed
  RenderContext + RenderSurface + Renderer wrapper). Identical manifest
  shape to hello-gradient's `HelloGradientRenderer` — single-source-of-
  truth emit template, only the struct name diverges. `aa` attribute
  omitted -> defaults to Area per R46.2.1 (UI-canonical mode).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloPathRenderer"
        backend="vello"/>
