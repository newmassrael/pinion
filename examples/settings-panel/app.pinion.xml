<?xml version="1.0" encoding="UTF-8"?>
<!--
  R667 §5.16 — pinion-forge renderer manifest for `settings-panel`.
  Emits `SettingsPanelRenderer` (Vello-backed). Same shape as
  todomvc; only the struct name diverges per the single-source emit
  template (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="SettingsPanelRenderer"
        backend="vello"/>
