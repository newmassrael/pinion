<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1445 §5.45 §5.27 layout-measured tail pin: pinion-forge renderer manifest
  for hello-transcript. Emits `HelloTranscriptRenderer` (Vello-backed). Same
  single-source-of-truth template as the other hello-* renderers; only the
  struct name diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloTranscriptRenderer"
        backend="vello"/>
