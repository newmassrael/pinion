<?xml version="1.0" encoding="UTF-8"?>
<!--
  R789 §3 §5.15 — own-rendered file manager: pinion-forge renderer manifest
  for hello-file-manager. Same single-source-of-truth template as the other
  hello-* renderers; only the struct name diverges. backend=vello.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloFileManagerRenderer"
        backend="vello"/>
