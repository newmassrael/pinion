<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1480 §5.15 §5.7 — renderer for the pre-encoded-answer demo. Same
  single-source emit template as hello-button's HelloButtonRenderer; only the
  struct name differs.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="EncodedAnswerRenderer"
        backend="vello"/>
