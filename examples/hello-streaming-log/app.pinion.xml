<?xml version="1.0" encoding="UTF-8"?>
<!--
  R996 §5.22 §5.23 §5.27 Model-View streaming-append: pinion-forge renderer
  manifest for hello-streaming-log. Emits `HelloStreamingLogRenderer`
  (Vello-backed). Same single-source-of-truth template as the other hello-*
  renderers; only the struct name diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloStreamingLogRenderer"
        backend="vello"/>
