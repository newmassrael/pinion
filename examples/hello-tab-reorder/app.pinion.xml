<?xml version="1.0" encoding="UTF-8"?>
<!--
  R743 §5.16 §5.51 — pinion-forge renderer manifest for
  hello-tab-reorder. Same single-source-of-truth template that emits the
  other hello-* renderers; only the struct name diverges. backend=vello.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloTabReorderRenderer"
        backend="vello"/>
