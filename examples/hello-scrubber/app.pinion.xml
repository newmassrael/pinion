<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1415 §5.16 pinion-forge renderer manifest for hello-scrubber. Same
  single-source-of-truth template that emits the other hello-* renderers;
  only the struct name diverges. backend=vello + default Area antialiasing.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloScrubberRenderer"
        backend="vello"/>
