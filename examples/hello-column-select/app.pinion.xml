<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1563 §5.40 a selection with two axes: pinion-forge renderer manifest for
  hello-column-select. Same single-source-of-truth template as the other
  hello-* renderers; only the struct name diverges. backend=vello +
  default Area antialiasing (R46.2.1).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloColumnSelectRenderer"
        backend="vello"/>
