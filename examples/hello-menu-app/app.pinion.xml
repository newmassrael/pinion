<?xml version="1.0" encoding="UTF-8"?>
<!--
  R832 — pinion-forge renderer manifest for hello-menu-app. Same
  single-source-of-truth template that emits the other hello-* renderers;
  only the struct name diverges. backend=vello.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloMenuAppRenderer"
        backend="vello"/>
