<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1395 numeric different-geometry cross-filter consumer: pinion-forge renderer
  manifest for hello-histogram-brush. Emits `HelloHistogramBrushRenderer`
  (Vello-backed RenderContext + RenderSurface + Renderer wrapper). Identical
  manifest shape to hello-linked-brush's `HelloLinkedBrushRenderer` —
  single-source-of-truth emit template, only the struct name diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloHistogramBrushRenderer"
        backend="vello"/>
