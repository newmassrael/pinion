<?xml version="1.0" encoding="UTF-8"?>
<!--
  R764 §5.22 — pinion-forge renderer manifest for hello-textarea.
  Emits `HelloTextAreaRenderer` (Vello-backed wrapper). Identical
  manifest shape to hello-textfield's renderer — only the struct name
  diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloTextAreaRenderer"
        backend="vello"/>
