<?xml version="1.0" encoding="UTF-8"?>
<!--
  R903 §5.22 — pinion-forge renderer manifest for hello-find-replace.
  Emits `HelloFindReplaceRenderer` (Vello-backed RenderContext +
  RenderSurface + Renderer wrapper). Identical manifest shape to
  hello-textfield's `HelloTextFieldRenderer` — single-source-of-truth
  emit template, only the struct name diverges. `aa` attribute omitted
  -> defaults to Area per R46.2.1 (UI-canonical mode).
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloFindReplaceRenderer"
        backend="vello"/>
