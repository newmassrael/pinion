<?xml version="1.0" encoding="UTF-8"?>
<!--
  R1396 docked-chart consumer: pinion-forge renderer manifest for
  hello-dock-chart. Emits `HelloDockChartRenderer` (Vello-backed RenderContext +
  RenderSurface + Renderer wrapper). Identical manifest shape to
  hello-chart-fill's `HelloChartFillRenderer` — single-source-of-truth emit
  template, only the struct name diverges.
-->
<pinion xmlns="https://pinion.dev/dsl/v1"
        kind="renderer"
        name="HelloDockChartRenderer"
        backend="vello"/>
